import os
import csv
import pandas as pd
import numpy as np
#def import_csv(csvfile):
data = []
Budget_data = os.path.join('..', 'Desktop', 'budget.csv') 
with open(Budget_data) as csvfile:
        csvreader = csv.reader(csvfile, delimiter=",")
        firstrow = next(csvreader)
        change = 0
        profit = 0
        diff = 0
        row_index = -1
        dude = int(row[1])
        for row in csvreader:
            if row:
                row_index +=1
                diff +=1
                change +=1
                columns = [str(row_index)]
                profit = profit + int(row[1])
                change = ((dude) - (change)) +(change)
                data.append(columns)
                #data.append(diff)
print("Total Month:", (diff))
print("Total Profit/Loss:$", (profit))
print(change)
